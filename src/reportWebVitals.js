// Uncomment the following line if you want to track web vitals
// import { reportWebVitals } from 'web-vitals';

// Function to log the results or send to an analytics endpoint
function reportWebVitals(metric) {
    console.log(metric);
  }
  
  export default reportWebVitals;
  